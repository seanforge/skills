# [MODULE OR SUBTREE NAME]

Scope: this file applies to `[relative/path/**]`.

## MUST-FOLLOW RULES

- **[AREA]:** [Required local action or constraint]. [Trigger or scope]. Source: [`path`].

## REPOSITORY MAP

1. **Domain modeling context:** [`path/to/context`]
2. **Development commands:** [`path/to/Makefile`], [`path/to/package.json`]
3. **Platform and release policy:** [`path/to/policy`]
